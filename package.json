{
  "name": "startup-investment-intelligence-platform",
  "version": "1.0.0",
  "description": "Startup Investment Intelligence Platform - MERN Stack",
  "scripts": {
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "start": "node server/server.js"
  },
  "keywords": ["mern", "investment", "startups", "vc", "intelligence"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
